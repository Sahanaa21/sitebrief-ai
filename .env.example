# Google Gemini API key (server-side only — never exposed to the browser).
# Get a free key at https://aistudio.google.com/app/apikey
# The app works without this key: it will automatically fall back to Demo Mode
# for the sample project, and will show a clear "live analysis unavailable"
# message with a Demo Mode suggestion for any real communication you paste.
GEMINI_API_KEY=
